project('pySpinW',
  version: '0.0.1' # TODO: write a script to get version from git tags
)

py = import('python').find_installation(pure: false)

py.install_sources([
    'CITATION.cff', 
    'LICENSE',
  ], 
  subdir: 'euphonic'
)

subdir('pyspinw')
